export enum Role {
  USER = 'USER',
  ADMIN = 'ADMIN',
  DEV = 'DEV',
}

export enum SyncStatus {
  PENDING = 'PENDING',
  SYNCED_CLIENT = 'SYNCED_CLIENT',
  SYNCED = 'SYNCED',
  SAVED = 'SAVED',
  ERROR = 'ERROR',
  DELETED = 'DELETED',
}
